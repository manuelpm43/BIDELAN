// Configuración común de acceso al backend de autenticación de BIDELAN.
// Mismo (sub)dominio que el visor: nginx hace proxy de /auth y /admin al backend.
const apiAuthUrl = 'https://visor.geospatiallab.xyz';
const claveTokenAuth = 'bidelanToken';
